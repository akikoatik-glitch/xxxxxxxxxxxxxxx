# --- junk / cache ---
.cache-xwhiz/
.build-classes.txt
*.log
.DS_Store
Thumbs.db

# --- local data caches (regenerated by the daily GitHub Action) ---
football/*.json
news/*.json

# --- node ---
node_modules/

# --- never commit secrets ---
.env
*.pem

# --- unrelated subprojects (kept locally, not part of the XWhiz site repo) ---
/wassil/
/forex-assistant/